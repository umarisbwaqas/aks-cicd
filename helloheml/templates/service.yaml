apiVersion: v1
kind: Service
metadata:
  name: {{ include "helloheml.fullname" . }}
  labels:
    {{- include "helloheml.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helloheml.selectorLabels" . | nindent 4 }}
